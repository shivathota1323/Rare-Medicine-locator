{
  "name": "rare-medicine-locator-atp",
  "version": "1.0.0",
  "private": true,
  "description": "MERN rare medicine locator app with separate Render backend and Vercel frontend.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install-all": "npm install && cd rare-medicine-backend && npm install && cd ../rare-medicine-frontend && npm install",
    "build": "cd rare-medicine-frontend && npm install && npm run build && cd ../rare-medicine-backend && npm install",
    "start": "cd rare-medicine-backend && npm start"
  },
  "keywords": [
    "mern",
    "medicine",
    "locator"
  ],
  "author": "",
  "license": "ISC"
}
